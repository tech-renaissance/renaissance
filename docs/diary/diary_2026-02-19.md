完成了RandomResizedCrop的测速。

命令是./test_pw_rrc --path /root/epfs/dataset/imagenet --resolution 224 --batch-size 256 --loaders 16 --preproc 96 --scale-min 0.08 --scale-max 1.0 --cpu-bind --format raw --lv 0

实测RAW（训练集+验证集）用时 **31.519 s**，DTS LV3（训练集+验证集）用时 **10.639 s**
