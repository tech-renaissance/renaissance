# 【用户评价】

DTensor初始化实现严重错误。

我就直接告诉你结论了。错得离谱，一点可采纳的地方都没有。

我们项目已经有了src/core/initializer.cpp，为什么不用呢？

你自己实现的这个初始化器多的是问题，自己看不出来吗？！你的kaiming_normal和xavier_normal是一模一样的实现，能正确吗？！你的随机数发生器用的是mt19937，随机可复现性考虑了吗？我们项目有专门为随机可复现性开发的高级随机数发生器rng.h和philox.h，你了解了吗？！我们的Tensor类就内置了使用rng.h的随机初始化功能，而Initializer就是直接使用Tensor类的这个功能，内聚而且准确，完全符合要求。你重复发明轮子，而且发明出来的轮子还是四方形的，不能跑！

